export default function ColorsLesson() {
    return (
        <div>
            <h1>🎨 CSS Colors</h1>
            <p>Colors in CSS can be defined by names, HEX, RGB, RGBA, HSL, or HSLA.</p>
             <h1>🎨 CSS Colors</h1>
            <p>Colors in CSS can be defined by names, HEX, RGB, RGBA, HSL, or HSLA.</p> <h1>🎨 CSS Colors</h1>
            <p>Colors in CSS can be defined by names, HEX, RGB, RGBA, HSL, or HSLA.</p> <h1>🎨 CSS Colors</h1>
            <p>Colors in CSS can be defined by names, HEX, RGB, RGBA, HSL, or HSLA.</p> <h1>🎨 CSS Colors</h1>
            <p>Colors in CSS can be defined by names, HEX, RGB, RGBA, HSL, or HSLA.</p> <h1>🎨 CSS Colors</h1>
            <p>Colors in CSS can be defined by names, HEX, RGB, RGBA, HSL, or HSLA.</p> <h1>🎨 CSS Colors</h1>
            <p>Colors in CSS can be defined by names, HEX, RGB, RGBA, HSL, or HSLA.</p> <h1>🎨 CSS Colors</h1>
            <p>Colors in CSS can be defined by names, HEX, RGB, RGBA, HSL, or HSLA.</p> <h1>🎨 CSS Colors</h1>
            <p>Colors in CSS can be defined by names, HEX, RGB, RGBA, HSL, or HSLA.</p> <h1>🎨 CSS Colors</h1>
            <p>Colors in CSS can be defined by names, HEX, RGB, RGBA, HSL, or HSLA.</p> <h1>🎨 CSS Colors</h1>
            <p>Colors in CSS can be defined by names, HEX, RGB, RGBA, HSL, or HSLA.</p> <h1>🎨 CSS Colors</h1>
            <p>Colors in CSS can be defined by names, HEX, RGB, RGBA, HSL, or HSLA.</p> <h1>🎨 CSS Colors</h1>
            <p>Colors in CSS can be defined by names, HEX, RGB, RGBA, HSL, or HSLA.</p> <h1>🎨 CSS Colors</h1>
            <p>Colors in CSS can be defined by names, HEX, RGB, RGBA, HSL, or HSLA.</p> <h1>🎨 CSS Colors</h1>
            <p>Colors in CSS can be defined by names, HEX, RGB, RGBA, HSL, or HSLA.</p>
            <pre>
                {`/* Example */
body {
  background-color: lightblue;
  color: #333;
}`}
            </pre>
        </div>
    );
}
